import { tailwind } from '@theme-ui/presets'

export const theme = {
    ...tailwind,
    fonts: {
        ...tailwind.fonts,
        body: 'IBM Plex Sans, sans-serif',
        heading: 'IBM Plex Sans, sans-serif',
    },
}
